## No spec-level changes

No existing requirements are modified, added, or removed by this change. Every observable behavior of the `url-shortening` capability remains identical. The switch from PostgreSQL to MySQL is a purely internal driver/infrastructure change with no effect on the spec-level contract.
